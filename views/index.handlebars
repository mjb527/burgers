
<img src="assets/img/burger.png" alt="Burger Image" class="img-fluid mx-auto d-block mt-4 mb-2">

<div class="row">
  <div class="col-sm-12 col-md-6 d-flex justify-content-center">

    <ul>

    {{#each burgers}}
      {{#unless devoured}}
        {{> burger/burger-block }}
      {{/unless}}
    {{/each}}
  </ul>

  </div>
  <div class="col-sm-12 col-md-6 d-flex justify-content-center">

    <ul>
      {{#each burgers}}
        {{#if devoured}}
          {{> burger/burger-block }}
        {{/if}}
      {{/each}}
    </ul>


  </div>

</div>


<div class="w100 text-center mb-5">
    <input type="text" name="burger-input" placeholder="Burger Name" id="burger-input">
    <button type="submit" class="btn-sm btn-outline-secondary" id="submit">Submit</button>
</div>
